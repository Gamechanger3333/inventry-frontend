# Backend API URL used by next.config.ts to proxy /api/* requests to your
# Express server. Must match whatever port your backend actually listens on
# (check its .env PORT value / its startup log line).
#
# NOTE: this only takes effect on `next dev` / `next build` startup - if you
# change it, restart the frontend dev server.
BACKEND_URL=http://localhost:8080
